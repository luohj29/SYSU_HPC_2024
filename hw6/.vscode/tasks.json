{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Nsight: autostart (localhost)",
            "command": "nvcc --compiler-options=-g,-fopenmp -G task3.cu -o task3",
            "problemMatcher": [
                "$nvcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}